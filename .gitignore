# Dependencies
node_modules/
.pnp
.pnp.js

# Build artifacts
dist/
build/
*.js.map

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun.lockb

# OS specific files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Extension specific
extension/*.js
!extension/content.js
!extension/background.js
# (Wait, if I'm building them from TS, I should probably ignore the .js files but keep the TS files. 
# But for now, since manifest needs them, I'll keep them out of gitignore unless I have a better build strategy.)
