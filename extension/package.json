{
  "name": "extension",
  "version": "1.0.0",
  "main": "background.js",
  "scripts": {
    "build": "esbuild background.ts content.ts --bundle --outdir=.",
    "watch": "esbuild background.ts content.ts --bundle --outdir=. --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
